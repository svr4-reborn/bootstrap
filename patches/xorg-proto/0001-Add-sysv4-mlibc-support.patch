diff --git a/include/X11/Xfuncs.h b/include/X11/Xfuncs.h
index b23c283..6e89e9e 100644
--- a/include/X11/Xfuncs.h
+++ b/include/X11/Xfuncs.h
@@ -37,14 +37,14 @@ in this Software without prior written authorization from The Open Group.
 void bzero();
 int bcmp();
 #  else
-#   if defined(SYSV) && !defined(__SCO__) && !defined(__sun) && !defined(__UNIXWARE__) && !defined(_AIX)
+#   if defined(SYSV) && !defined(__SCO__) && !defined(__sun) && !defined(__UNIXWARE__) && !defined(_AIX) && !defined(__svr4__)
 #    include <memory.h>
 void bcopy();
 #    define bzero(b,len) memset(b, 0, len)
 #    define bcmp(b1,b2,len) memcmp(b1, b2, len)
 #   else
 #    include <string.h>
-#    if defined(__SCO__) || defined(__sun) || defined(__UNIXWARE__) || defined(__CYGWIN__) || defined(_AIX) || defined(__APPLE__)
+#    if defined(__SCO__) || defined(__sun) || defined(__UNIXWARE__) || defined(__CYGWIN__) || defined(_AIX) || defined(__APPLE__) || defined(__svr4__)
 #     include <strings.h>
 #    endif
 #    define _XFUNCS_H_INCLUDED_STRING_H
diff --git a/include/X11/Xos.h b/include/X11/Xos.h
index 9b34a09..38937f3 100644
--- a/include/X11/Xos.h
+++ b/include/X11/Xos.h
@@ -60,7 +60,7 @@ in this Software without prior written authorization from The Open Group.
  */
 
 # include <string.h>
-# if defined(__SCO__) || defined(__UNIXWARE__) || defined(__sun) || defined(__CYGWIN__) || defined(_AIX) || defined(__APPLE__) || defined(__FreeBSD__) || defined(__OpenBSD__)
+# if defined(__SCO__) || defined(__UNIXWARE__) || defined(__sun) || defined(__CYGWIN__) || defined(_AIX) || defined(__APPLE__) || defined(__FreeBSD__) || defined(__OpenBSD__) || defined(__svr4__)
 #  include <strings.h>
 # else
 #  ifndef index
diff --git a/include/X11/Xos_r.h b/include/X11/Xos_r.h
index f963b64..b8c826e 100644
--- a/include/X11/Xos_r.h
+++ b/include/X11/Xos_r.h
@@ -318,7 +318,7 @@ static __inline__ void _Xpw_copyPasswd(_Xgetpwparams p)
   (_Xos_processUnlock), \
   (p).pwp )
 
-#elif !defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(__APPLE__)
+#elif !defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(__APPLE__) && !defined(__svr4__)
 # define X_NEEDS_PWPARAMS
 typedef struct {
   struct passwd pws;
diff --git a/include/X11/Xosdefs.h b/include/X11/Xosdefs.h
index ebc993c..59e2118 100644
--- a/include/X11/Xosdefs.h
+++ b/include/X11/Xosdefs.h
@@ -43,7 +43,7 @@ in this Software without prior written authorization from The Open Group.
 # ifdef __i386__
 #  ifdef SYSV
 #   if !defined(__SCO__) && \
-	!defined(__UNIXWARE__) && !defined(__sun)
+	!defined(__UNIXWARE__) && !defined(__sun) && !defined(__svr4__)
 #    if !defined(_POSIX_SOURCE)
 #     define X_NOT_POSIX
 #    endif
